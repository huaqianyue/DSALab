# DSALab 软件开发说明

> 本文档基于实际源码编写，不依赖 README.md 的描述。部分 README 中过时或失真的信息在文末「与 README 的差异」一节单独说明。

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [目录结构](#3-目录结构)
4. [系统架构](#4-系统架构)
5. [进程间通信（IPC）](#5-进程间通信ipc)
6. [核心功能模块](#6-核心功能模块)
   - [6.1 题目管理与同步](#61-题目管理与同步)
   - [6.2 代码编辑](#62-代码编辑)
   - [6.3 编译与运行](#63-编译与运行)
   - [6.4 调试](#64-调试)
   - [6.5 自动测试判题](#65-自动测试判题)
   - [6.6 音频录制与播放](#66-音频录制与播放)
   - [6.7 学习历史记录](#67-学习历史记录)
   - [6.8 成果导出](#68-成果导出)
   - [6.9 设置与主题](#69-设置与主题)
7. [数据存储与文件布局](#7-数据存储与文件布局)
8. [构建与打包](#8-构建与打包)
9. [配置项](#9-配置项)
10. [与 README 的差异](#10-与-readme-的差异)

---

## 1. 项目概述

DSALab 是一个面向「数据结构与算法」课程学习的 C++ 编程桌面应用，本质上是 **Electron 桌面程序**：以 Angular 渲染界面，以 Monaco Editor 提供代码编辑能力，以 MinGW-w64（g++/gdb）提供编译、运行与调试能力。

围绕「算法题练习」这一场景，它在通用代码编辑器之上叠加了一套完整的学习闭环：

```
选题 → 读题 → 写代码 → 编译运行 → 调试 → 自动判题评分 → 录音讲解 → 打包导出
```

其数据模型以「题目（Problem）」为核心：每个题目携带题面、学生调试模板、判题模板、函数签名，学生在题目工作区里产生的代码、录音、测试结果、操作历史都持久化到本地文件系统。

> 注：包名与作者信息为 `dsalab` / `huaqianyue`（见 `package.json`），题目 CDN 指向 `huaqianyue/DSALab` 仓库；本项目目录名为 `DSALab-main`。README 中「与 Bloodshed 的 Dev-C++ 无关联」的声明与代码一致——项目仅复用了 Dev-C++ 的 `ConsolePauser` 子程序（GPL v2）。

---

## 2. 技术栈

| 层次 | 技术 | 版本 | 用途 |
|------|------|------|------|
| 桌面壳 | Electron | ^11.1.0 | 桌面运行时，主进程/渲染进程 |
| 前端框架 | Angular | 12.1.1 | 渲染进程 UI 框架 |
| 语言 | TypeScript | ~4.3.4 | 全项目语言 |
| UI 组件库 | ng-zorro-antd | ^11.4.2 | Ant Design 风格的 Angular 组件 |
| 图标 | @ant-design/icons-angular | ^20.0.0 | 图标集 |
| 编辑器 | Monaco Editor | via `@materia-ui/ngx-monaco-editor` 5.0.0 | VS Code 同源编辑器 |
| 调试器 | @gytx/tsgdbmi | ^0.3.2 | GDB/MI 协议的 TypeScript 封装 |
| 配置持久化 | electron-store | ^7.0.2 | 主进程配置存取 |
| 类型化 IPC | electron-typed-ipc | ^0.1.0 | 类型安全的 IPC 定义 |
| 编码转换 | iconv-lite | ^0.6.2 | gcc 输出 / Windows 代码页转换 |
| 压缩导出 | archiver | ^5.3.1 | ZIP 打包 |
| 分屏布局 | angular-split | ^5.0.0 | 可拖拽分栏 |
| 音频播放 | Howler.js | 2.2.3（运行时 CDN 加载） | 录音回放 |
| 编译器 | MinGW-w64 g++ | 11.1.0（内置或自定义） | 编译/链接，C++20 |
| 构建 | Webpack 5 + ts-loader | 5.24.2 | 主进程打包 |
| 打包 | electron-builder | ^22.9.1 | 生成 Windows NSIS 安装包 |

关键依赖说明：
- **Monaco** 通过 `@materia-ui/ngx-monaco-editor` 引入，`angular.json` 中把 `monaco-editor-core` 的静态资源复制到 `assets/monaco-editor/`。
- **Howler.js** 不在 npm 依赖中，而是运行时从 `cdnjs.cloudflare.com` 动态注入 `<script>`（见 `howler-audio-player.component.ts`）。
- **MinGW-w64** 不作为 npm 包，而是放在 `src/extraResources/mingw64`，由 electron-builder 打入安装包。

---

## 3. 目录结构

```
DSALab-main/
├── main.ts                        # Electron 主进程入口（创建窗口）
├── main.webpack.js                # 主进程 webpack 配置（打包 background）
├── angular.webpack.js             # Angular 自定义 webpack 配置（target/fallback）
├── angular.json                   # Angular CLI 构建配置
├── package.json                   # 依赖与 npm 脚本
├── problem.json                   # 题库数据（约 159 KB，内置默认题目）
├── builder/
│   ├── default.json               # electron-builder 默认打包配置
│   ├── no-mingw.json              # 不含 MinGW 的打包
│   └── with-mingw-zip.json        # 含 MinGW 的 ZIP 打包
└── src/
    ├── background/                # Electron 主进程业务逻辑（经 webpack 打包）
    │   ├── index.ts               # 挂载所有 handler
    │   ├── basicUtil.ts           # 公共工具：路径、store、MinGW 路径、类型化 IPC
    │   └── handlers/
    │       ├── build.ts           # 编译 / 运行 / 取消运行
    │       ├── debug.ts           # GDB 调试
    │       ├── file.ts            # 文件打开/保存对话框与读写
    │       ├── store.ts           # 配置读写（带校验）
    │       ├── encode.ts          # 代码页查询/校验
    │       ├── theme.ts           # 编辑器主题加载
    │       ├── window.ts          # 窗口控制
    │       └── dsalab.ts          # DSALab 核心：题目/历史/测试/导出
    ├── app/                       # Angular 渲染进程
    │   ├── app.module.ts          # 根模块
    │   ├── app-routing.module.ts  # 根路由（聚合各子路由）
    │   ├── core/                  # 核心层
    │   │   ├── services/electron.service.ts   # IPC 桥接服务
    │   │   └── ipcTyping.d.ts     # 全项目 IPC 类型定义（单一事实来源）
    │   ├── services/              # 业务服务层（见 §4.4）
    │   ├── header/                # 顶部菜单栏 + DSALab 控制 + 编译运行按钮
    │   ├── main-view/             # 主视图
    │   │   ├── sidebar/           # 左侧栏：题目列表 / 题目描述 / 调试查看
    │   │   ├── tabs/              # 中间编辑器标签页 + 设置页
    │   │   └── tools/             # 底部面板：问题 / 输出 / 调试 / 测试结果
    │   ├── shared/components/howler-audio-player/  # Howler 音频播放器
    │   ├── configs/               # cppLanguageConfig / editorKeybindings
    │   └── assets/i18n/           # 国际化资源（en.json）
    ├── extraResources/            # 打包进安装包的外部资源
    │   ├── mingw64/               # MinGW-w64 编译器（需自行放置）
    │   ├── bin/ConsolePauser.exe  # 运行后暂停控制台（源自 Dev-C++）
    │   ├── themes/                # Monaco 主题 JSON（classic/dark_plus/light_plus/monokai）
    │   └── anon_workspace/        # 匿名临时工作区
    └── environments/              # Angular 环境配置（dev/prod/web）
```

---

## 4. 系统架构

### 4.1 双进程模型

应用采用 Electron 标准的主/渲染双进程架构：

```
┌─────────────────────────────────────────────────────────┐
│                     Electron 主进程                      │
│  main.ts ──► background-bundle.js（webpack 打包）         │
│  ├─ 创建 BrowserWindow（nodeIntegration: true）           │
│  └─ handlers/: 编译/运行/调试/文件/存储/DSALab 业务        │
└───────────────────────▲──────────────┬───────────────────┘
                    invoke()        webContents.send()
                    （请求/响应）      （事件推送）
┌───────────────────────┴──────────────▼───────────────────┐
│                    渲染进程（Angular）                    │
│  ElectronService ──► 各业务 Service ──► 各 Component      │
└─────────────────────────────────────────────────────────┘
```

- 窗口配置（`main.ts`）：`nodeIntegration: true`、`contextIsolation: false`、`allowRunningInsecureContent: false`，应用启动时 `Menu.setApplicationMenu(null)` 隐藏系统菜单。窗口就绪后延迟 400ms 创建（规避透明窗口黑屏问题）。
- 主进程入口 `main.ts` 末尾 `import './src/background'`，把 `src/background/handlers` 全部加载注册；`background/index.ts` 中 `global['langServerProcess']` 为语言服务预留位。

### 4.2 类型化 IPC

项目用 `electron-typed-ipc` 把 IPC 的两端都做类型约束，`src/app/core/ipcTyping.d.ts` 是唯一类型来源，同时被主进程（`background/ipcTyping.d.ts` re-export）和渲染进程引用：

- `IpcCommands`：渲染进程 `invoke` 的请求 → 主进程 `handle` 的响应（异步请求/响应）。
- `IpcEvents`：主进程 `webContents.send` 推送给渲染进程 `ipcRenderer.on` 的事件。

### 4.3 主进程（background/handlers）

| 模块 | 职责 |
|------|------|
| `build.ts` | 调用 g++ 编译（先 `.o` 后 `.exe`）、运行 exe（经 ConsolePauser）、取消运行 |
| `debug.ts` | 用 `GdbController` 管理 GDB 生命周期，转发 console/notify/result |
| `file.ts` | 打开/保存/另存对话框、文件读写、目录选择对话框 |
| `store.ts` | 配置 get/set/reset，`advanced.ioEncoding` 有编码合法性校验 |
| `encode.ts` | 获取系统 ANSI 代码页并映射到 iconv 编码名 |
| `theme.ts` | 枚举/加载/校验 `extraResources/themes` 下的主题 JSON |
| `window.ts` | DevTools、标题、命令行参数、资源路径 |
| `dsalab.ts` | DSALab 全部核心逻辑（题目同步/导入导出/历史/测试/设置），约 1400 行 |

### 4.4 渲染进程服务层（app/services）

| 服务 | 职责 |
|------|------|
| `electron.service.ts` | 检测是否在 Electron 环境，封装 `invoke`/`on`，提供 `getConfig`/`setConfig` |
| `tabs.service.ts` | 标签页模型（file/setting/welcome），管理激活、增删、代码同步 |
| `editor.service.ts` | Monaco 编辑器封装：模型切换、断点装饰、C++ 语法高亮注册、撤销/重做与 IME 检测 |
| `file.service.ts` | 文件的新建/打开/保存/另存/定位（跳转到某行） |
| `build.service.ts` | 编译/运行/测试的调度，监听编译与程序退出事件，记录历史 |
| `debug.service.ts` | 调试状态机：断点、单步、调用栈、局部变量、表达式求值 |
| `problems.service.ts` | 编译诊断（问题列表）、链接错误、未知错误的可观察状态 |
| `watch.service.ts` | 调试变量树的数据源（基于 CDK FlatTree） |
| `status.service.ts` | 命令注册表（菜单/快捷键统一入口），`Command` 含 enable/run |
| `hotkeys.service.ts` | 基于 Angular EventManager 的快捷键绑定 |
| `settings.service.ts` | 设置页的抽象（编译设置/编辑器设置两个子设置） |
| `theme.service.ts` | 主题切换：加载 CSS、映射为 Monaco 主题 |
| `dsalab-problem.service.ts` | 题目/工作区的状态中枢（BehaviorSubject 驱动） |
| `dsalab-history.service.ts` | 历史事件记录的渲染进程侧封装 |
| `dsalab-settings.service.ts` | 用户信息（姓名/学号/最后打开的题目） |
| `dsalab-paths.service.ts` | 工作区路径的渲染进程侧封装 |

---

## 5. 进程间通信（IPC）

### 5.1 请求/响应命令（IpcCommands）

**文件类**：`file/save`、`file/saveAs`、`file/open`、`file/openDirectoryDialog`，以及 `dsalab.ts` 中的 `file/openFile`（打开文件并返回内容）。

**构建类**：`build/build`、`build/runExe`、`build/cancelRun`。

**调试类**：`debug/start`、`debug/exit`、`debug/sendRequest`。

**窗口/编码/主题/存储**：`window/*`、`encode/*`、`theme/*`、`store/*`。

**DSALab 类**（在 `dsalab.ts` 中直接 `ipcMain.handle` 注册）：

| 命令 | 功能 |
|------|------|
| `dsalab-get-problems` | 立即返回本地题目，后台异步拉取 CDN 合并 |
| `dsalab-refresh-problems` | 主动刷新：拉 CDN 合并并回写本地 |
| `dsalab-import-problems` | 从 JSON 导入题目并合并 |
| `dsalab-export-problems` | 筛选题目打包 ZIP（带保存对话框） |
| `dsalab-read-problem-code` | 读某题 `code.cpp`（缺失则回退调试模板） |
| `dsalab-read-problem-audio` | 读某题 `audio.webm` 为 ArrayBuffer |
| `dsalab-save-problem-workspace` | 保存代码 + 音频，并记录历史 |
| `dsalab-record-history` | 接收渲染进程发来的历史事件（`ipcMain.on`） |
| `dsalab-save-problems-to-local` | 渲染进程回写本地题库 |
| `dsalab-get-pure-local-problems` / `dsalab-read-pure-local-problems` | 读纯本地题库（不合并 CDN） |
| `dsalab-get-workspace-root` | 返回工作区根目录 |
| `dsalab-load-settings` / `dsalab-save-settings` | 应用设置读写 |
| `dsalab-run-test` | 执行自动判题（见 §6.5） |
| `dsalab-read-test-result` | 读某题的 `test-result.json` |

### 5.2 主进程推送事件（IpcEvents）

| 事件 | 触发时机 |
|------|----------|
| `ng:build/buildStarted` / `ng:build/buildComplete` | 编译开始 / 编译完成（携带 `BuildResult`） |
| `ng:debug/debuggerStarted` / `debuggerStopped` | GDB 启动 / 停止 |
| `ng:debug/console` / `notify` / `result` | GDB 输出 / 通知 / 结果 |
| `ng:program/exit` / `ng:program/error` | 程序运行结束 / 出错（含退出码、时长） |
| `ng:dsalab/cdn-loading` / `cdn-loaded` / `cdn-failed` | CDN 题库拉取的三阶段状态 |

---

## 6. 核心功能模块

### 6.1 题目管理与同步

**数据来源三层**：本地 `problems.json`（持久化）+ CDN 题库（拉取后合并）+ 用户导入。

- **CDN 多镜像容错**（`dsalab.ts`）：`CDN_PROBLEMS_URLS` 配置了 3 个地址（GitHub raw + 两个 jsDelivr 镜像），逐个尝试，单个请求 8 秒超时，全部失败则回退本地。
- **加载策略**：`dsalab-get-problems` 先读本地立即返回（保证离线可用、启动快），随后异步拉 CDN；成功后用 `mergeProblemLists` 合并，再通过 `ng:dsalab/cdn-loaded` 通知前端刷新。
- **合并规则**（`mergeProblemLists`）：
  - CDN 有而本地没有的题目 → 新增，工作区字段置空。
  - 两边都有的题目 → 用 CDN 更新题面与模板，**保留**本地的 `Code`/`Audio`/`testStatus`/`testScore`。
  - 仅本地有的题目 → 原样保留。
  - 无效题目（缺 `id`/`shortDescription`/`fullDescription`）被跳过。
- **导入**：`convertToProblem` 做字段校验后走同一套合并逻辑，统计并返回跳过数量。
- **题目列表**：按 `id` 字符串排序（`localeCompare`）。

**题目字段**（见 `dsalab-types.ts` 的 `Problem`）：

```ts
{
  id: string;                 // 唯一标识
  Title: string;              // 标题（冗余，等于 shortDescription）
  shortDescription: string;   // 短描述
  fullDescription: string;    // 完整题面（Markdown）
  Audio: string;              // 是否有音频（非空表示有）
  Code: string;               // 是否有代码（非空表示有）
  studentDebugTemplate?: string;  // 学生调试模板（本地调试入口 main）
  judgeTemplate?: string;         // 判题模板（含 {{STUDENT_CODE}} 占位符）
  functionSignature?: string;     // 函数/类签名，用于提取学生代码
  testStatus?: 'passed' | 'failed' | 'not_tested';
  testScore?: number;             // 0-100
}
```

### 6.2 代码编辑

- **Monaco 封装**（`editor.service.ts`）：
  - 每个标签页对应一个 `monaco.Uri` 与一个 model，`switchToModel` 负责模型切换并保存/恢复光标与滚动位置。
  - 断点用 `deltaDecorations` 实现，点击 gutter 增删断点。
  - 调试「当前行」用 `showTrace`/`hideTrace` 的装饰实现。
  - 注册 C++ 语法高亮（`configs/cppLanguageConfig.ts` 的 Monarch token 与语言配置），并维护一份完整 C++ 标准头文件清单用于扩展名判定。
- **缺失编辑器动作补齐**：`addMissingActions` 补上了 Electron 环境下 Monaco 缺失的 undo/redo/剪贴板/全选动作（引用 Monaco 上游 issue 的 workaround）。
- **撤销/重做与 IME 检测**：`editor.service.ts` 监听键盘事件识别 `Ctrl+Z/Y`、监听 `compositionstart/end` 识别中文输入法，并把每次内容变更细分为 `type / ime_input / paste_insert / paste_replace / delete / undo_redo` 等操作类型，供历史记录使用（见 §6.7）。

### 6.3 编译与运行

编译链路（`build.ts`）分两步，**先编译后链接**：

1. **编译成目标文件**：调用 `g++ -c`，附加 `-fdiagnostics-format=json`，让 gcc 输出结构化诊断 JSON。
2. **链接成可执行文件**：再次调用 `g++`，静态链接 `-static-libgcc -static-libstdc++`，产物为同目录同名 `.exe`。

关键实现点：

- **动态编译参数**：配置项 `build.compileArgs` 中可含 `DYN-fexec-charset`，在真正编译时被替换为 `-fexec-charset=<ioEncoding>`（解决中文输出乱码）。
- **g++ 路径**：`getMingwPath()` 依据 `env.useBundledMingw` 在「内置 MinGW」与「自定义路径」间选择；编译前检查 `bin/g++.exe` 是否存在，不存在则返回 `compiler_not_found` 阶段错误。
- **诊断解析**：`BuildResult` 的 `stage` 区分 `compile / link / parse_error / compiler_not_found`，前端据此在「问题面板」或「输出面板」展示。
- **运行**：不直接运行 exe，而是 `spawn("...ConsolePauser.exe", [exePath])`，由 ConsolePauser 拉起目标程序并在结束后暂停控制台（detached + shell）。`ConsolePauser` 源自 Dev-C++（GPL v2），内部把控制台切到 UTF-8 并打印「Process exited after X seconds with return value N」。
- **增量编译**：`runExe` 中若 exe 已存在且比源文件新，则跳过编译直接运行。
- **取消运行**：`build/cancelRun` 在 Windows 上用 `taskkill /T /F` 杀进程树，其他平台 `SIGKILL`。
- **DSALab 联动**：当活动标签页是 `dsalab-*` 时，编译/运行/测试前会先 `syncActiveCode` → `updateCurrentProblemCode` → `saveCurrentProblem`，把编辑器内容落盘后再操作（`build.service.ts`）。

### 6.4 调试

调试基于 GDB/MI 协议，通过 `@gytx/tsgdbmi` 的 `GdbController`（`debug.ts`）：

- **启动**：先以 `-g` 编译，再 `gdb.launch(gdb.exe, [exe], {cwd})`，随后执行启动命令 `-gdb-set new-console on`、`-enable-pretty-printing`。
- **输出转发**：GDB 的 `console` / `notify` / `result` 三类响应分别转发到渲染进程；`notify` 时调用 `window.focus()`。
- **断点**：启动时把编辑器中的断点（含条件表达式 `-c`、命中次数 `-i`）批量 `-break-insert`。
- **单步/控制**（`debug.service.ts`）：`continue/next/step/finish/run` 映射为 `-exec-continue/-exec-next/-exec-step/-exec-finish/-exec-run`。
- **数据获取**：调用栈 `-stack-list-frames`、局部变量 `-stack-list-variables --all-values`、变量创建 `-var-create`、子变量 `-var-list-children`、更新 `-var-update`、表达式求值 `-data-evaluate-expression`、命令行 `-interpreter-exec console`。
- **停止定位**：解析 `notify` 中的 `breakpoint-hit / end-stepping-range / function-finished / exited` 等 reason，把停止位置经 `traceLine`（100ms 防抖）驱动编辑器高亮当前行。
- **断点 UI**：编辑器 gutter 增删断点 → `breakpointInfos$` 同步到调试面板；调试面板也支持编辑断点条件。
- **健壮性**：`sendMiRequest` 带 token 匹配与 2 秒超时，超时则弹窗提示「GDB 未响应」并退出；`debug.service.ts` 过滤 GDB 启动阶段的噪音输出（前 30 行）。

### 6.5 自动测试判题

判题流程（`dsalab.ts` 的 `dsalab-run-test`）：

1. 读题目的 `judgeTemplate`（无则报错「该题目未配置测试模板」）。
2. 读学生 `code.cpp`。
3. `CppFunctionExtractor.extractClassDefinition` 依据 `functionSignature`（如 `class Solution`）从学生代码中**按花括号配对**提取类定义（`class Xxx { ... }`，含尾随分号）。
4. 用提取出的代码替换判题模板中的 `{{STUDENT_CODE}}` 占位符，生成完整判题源码。
5. `compileAndRunJudge` 把判题代码写到临时目录（`temp/DSALab-cpp/{id}_judge.cpp`），用 g++ 编译（10s 超时），再运行 exe（5s 超时）。
6. `parseTestResult` 解析输出中的约定标记：
   - `[RESULT] x/y tests passed` → 通过数/总数
   - `[SCORE] n` → 分数
7. 写回 `test-result.json`，更新题库中该题的 `testStatus` / `testScore`，返回结果给前端。

前端（`build.service.ts` 的 `runTest`）负责：保存代码 → 记录 `test_start` → 调用判题 → 切换到底部「测试结果」面板 → 通过 `window.dispatchEvent('testResultUpdated')` 通知结果组件 → 记录测试结果历史 → 刷新题目列表。

> 判题的正确性依赖判题模板输出 `[RESULT]` 与 `[SCORE]` 标记的约定，这是题目数据与判题器之间的「契约」。

### 6.6 音频录制与播放

- **录制**（`problem-description.component.ts`）：使用浏览器 `navigator.mediaDevices.getUserMedia` + `MediaRecorder` API（README 所述「WebRTC」即此），录制为 `audio/webm`。支持开始/暂停/恢复/停止，录制时长按「总时长 - 暂停时长」计算，并用 100ms 定时器刷新显示。
- **落盘**：停止录制后生成 Blob，经 `dsalab-save-problem-workspace` 把 `audio.webm` 写入工作区（渲染进程把 Blob 转为 ArrayBuffer 传给主进程）。
- **播放**（`howler-audio-player.component.ts`）：Howler.js 从 CDN 动态加载，封装成可复用组件，提供播放/暂停/停止/进度拖拽/音量，`requestAnimationFrame` 平滑刷新进度条；通过 `@Output` 事件把时长、播放状态回传给父组件用于记录历史。

### 6.7 学习历史记录

这是 DSALab 最具特色、也最复杂的一块，跨主/渲染两进程协作：

- **渲染进程**（`dsalab-history.service.ts`）：把「代码编辑、程序运行、音频、测试、调试」等行为封装为统一的 `HistoryEvent`，通过 `dsalab-record-history`（`ipcMain.on`，单向发送）推给主进程。
- **主进程**（`dsalab.ts` 的 `recordHistoryEventInternal`）：对事件做**缓冲与合并**后写入 `history.json`：

| 机制 | 参数 | 说明 |
|------|------|------|
| 批量缓冲 | 20s（`HISTORY_FLUSH_BATCH_INTERVAL_MS`） | 普通编辑事件攒批落盘，减少 IO |
| 字符级合并 | type 10s / delete 2s | 连续的 `type`/`delete` 操作按位置连续性合并成一个事件 |
| 运行事件缓冲 | 独立 `runEventsBuffer` | `program_output/error/user_input` 攒批，`run_end` 时一并落盘 |
| 关键事件即时落盘 | — | `run_start`、`problem_loaded/saved/switched`、调试控制等立即刷新 |
| 退出兜底 | `app.on('before-quit')` | 退出前 flush 所有缓冲区 |

- **编辑事件的精细分类**：`editor.service.ts` 识别撤销/重做、IME 输入、粘贴插入/替换、删除等，并计算被删除文本、修正 Monaco 的范围/光标位置（详见 `recordDSALabCodeEditHistory`）。
- **事件类型全集**：`edit`、`run_start/run_end/compile_error/run_timeout/program_terminated_by_new_run`、`program_output/program_error/user_input`、`problem_loaded/saved/switched`、`audio_record_start/pause/resume/stop/play`、`test_start/test_completed/test_failed`，以及 `debug_*` 系列（按钮点击、启动/退出、单步、断点增删改、控制台输出、表达式求值等）。

### 6.8 成果导出

- **导出条件**（`dsalab-control.component.ts`）：仅当题目**同时**具备代码、音频、测试结果（`testStatus` 非 `not_tested` 且有 `testScore`）才可导出。
- **导出交互**：模态框列出可导出题目，支持勾选、全选、自动定位当前题目到列表中间；强制填写姓名与学号。
- **文件名生成**：`学号_姓名_YYYYMMDD_HHMMSS.zip`；后端（`dsalab.ts`）会进一步在时间戳前**插入各题测试分数**（如 `..._90_100_NA_20260831_...zip`），并对非法字符做清洗。
- **打包**：`archiver` 以最高压缩级别（`zlib level 9`）把每道题的 `code.cpp`、`audio.webm`、`history.json`、`test-result.json` 按 `{problemId}/` 目录结构打入 ZIP，弹系统保存对话框选择位置（默认 `~/Downloads`）。

### 6.9 设置与主题

- **配置项**（electron-store，`basicUtil.ts` 默认值）：

| key | 默认值 | 说明 |
|-----|--------|------|
| `build.compileArgs` | `['-g','-std=c++20','DYN-fexec-charset']` | 编译参数（可含 `DYN-` 动态项） |
| `env.mingwPath` | `''` | 自定义 MinGW 路径 |
| `env.useBundledMingw` | 依据 `mingw64` 是否存在 | 是否使用内置 MinGW |
| `advanced.ioEncoding` | `cp936` | 输入输出编码（中文） |
| `theme.active` | `Light_plus` | 当前主题 |

- **设置页**：`settings.service.ts` 抽象出「编译设置（sfb 编译参数 + env 环境）」与「编辑器设置（theme 主题）」两个子设置，作为特殊标签页打开（`~build`、`~editor`）。`SfbOptions` 类把「标准/优化/警告/动态参数」等结构化选项与 gcc 命令行字符串互转。
- **主题**：`theme.service.ts` 同时切换（a）ng-zorro 的 light/dark CSS（`angular.json` 里预打包了 `ng-zorro-antd.min.css` 与 `.dark.min.css` 两个 bundle）与（b）Monaco 编辑器主题；`extraResources/themes/*.json` 定义颜色、粗体/斜体/下划线 token，`theme.ts` 做格式校验，缺失时回退 `classic.json`。
- **首次环境配置**：`app.component.ts` 启动时若「未使用内置 MinGW 且未配置自定义路径」则弹出环境设置模态框。

---

## 7. 数据存储与文件布局

所有路径由 `DSALabPaths`（主进程 `dsalab.ts`）与 `DSALabPathsService`（渲染进程）定义：

| 路径 | 内容 |
|------|------|
| `<userData>/DSALab/problems.json` | 本地题库（与 CDN 合并后的结果） |
| `<documents>/DSALab Workspaces/<problemId>/code.cpp` | 学生代码 |
| `<documents>/DSALab Workspaces/<problemId>/audio.webm` | 录音 |
| `<documents>/DSALab Workspaces/<problemId>/history.json` | 学习历史（事件数组） |
| `<documents>/DSALab Workspaces/<problemId>/test-result.json` | 测试结果 |
| `<userData>/DSALab/settings.json` | 应用设置（姓名/学号/最后打开题目） |
| `<temp>/DSALab-cpp/` | 判题临时源码与可执行文件 |
| `<resources>/extraResources/` | 打包后的 MinGW、ConsolePauser、主题 |

- `<userData>` 即 Electron `app.getPath('userData')`；`<documents>` 即 `app.getPath('documents')`。
- `problems.json` 中每个问题的 `Code`/`Audio` 字段仅存「文件名」或空串，实际内容存在工作区目录，两者通过 `saveProblemWorkspace` 联动更新。
- `extraResourcesPath` 在开发态指向 `src/extraResources`，打包态指向安装目录（用 `electron-is-running-in-asar` 区分）。

---

## 8. 构建与打包

### 8.1 npm 脚本（package.json）

| 命令 | 作用 |
|------|------|
| `npm start` | `ng serve -o` 浏览器热重载（调试 UI，无 Electron 能力） |
| `npm run electron:dev` | 开发模式：并行构建 Angular + 打包主进程，再启动 Electron |
| `npm run electron:prod` | 生产模式运行 |
| `npm run build` | 生产构建 + electron-builder（NSIS 安装包） |
| `npm run build:no-mingw` / `build:mingw-zip` | 不含/含 MinGW 的打包变体 |
| `npm test` | Karma 单元测试 |
| `npm run lint` | ESLint |

### 8.2 构建链路

```
Angular (angular.json, @angular-builders/custom-webpack)
   └─► dist/  （渲染进程资源 + index.html）
主进程 (main.webpack.js, ts-loader)
   └─► dist/background-bundle.js
electron-builder (builder/*.json)
   └─► release/  NSIS 安装包
```

- `angular.webpack.js` 把 target 设为 `electron-renderer`，并对 Node 内建模块（`fs`/`crypto`/`os` 等）配置 `resolve.fallback`；`path` 用 `path-browserify` 兜底。
- `package.json` 的 `main` 字段指向 `dist/background-bundle.js`，即打包后的主进程产物。
- `builder/default.json`：产物名 DSALab，`extraResources` 把 `src/extraResources` 全量打入；NSIS 安装器（非 oneClick，perMachine），并注册 `.cc/.cxx/.cpp/.h` 文件关联。
- **内置编译器**：`mingw64` 需要按 `src/extraResources/README.md` 说明手动解压放置（源自 `mingw-release` 的 11.1.0 posix-seh 版本），`build:mingw-zip` 会把编译器一并打包。

### 8.3 运行环境

- 目标平台以 **Windows** 为主（编译/运行/调试均调用 `g++.exe`/`gdb.exe`/`ConsolePauser.exe`，取消运行用 `taskkill`）；`main.ts` 仍保留了 macOS 的 `activate`/`window-all-closed` 处理。
- Node 引擎要求 `>=16`（`engines` 字段）；`browserslist` 针对 `Electron 11.1` 与 `Node 16`。

---

## 9. 配置项

运行时配置集中在 electron-store（见 §6.9），其余配置来自：

- **编译参数**：`build.compileArgs`（字符串数组，`SfbOptions` 负责与设置 UI 互转）。
- **编码**：`advanced.ioEncoding` 默认 `cp936`，实际编译时经 `DYN-fexec-charset` 注入 `-fexec-charset`；`encode/getAcp` 通过 `chcp`（当前为绕过 `chcp` 模块的临时实现，固定返回 936）查询系统代码页。
- **主题**：`theme.active`，可选值来自 `extraResources/themes` 下的 JSON 文件名。

---

## 10. 与 README 的差异

基于代码核对，README 存在以下与实现不一致或需补充之处：

1. **版本号**：README「版本信息」写 `1.1.0`，但 `package.json` 的 `version` 为 `2.0.0`。
2. **音频技术描述**：README 称「使用 WebRTC 技术进行音频录制」，实现上用的是 `MediaRecorder` API（`getUserMedia` + `MediaRecorder`），README 的「WebRTC」是宽泛指代。
3. **国际化的实际状态**：README 称「仅支持简体中文界面」。代码确实引入了 `@ngx-translate` 与 `assets/i18n/en.json`，但**绝大多数 UI 文案为硬编码中文**（组件模板与 `message.success(...)` 等），i18n 框架基本未真正启用。
4. **README 未覆盖的机制**（代码中存在且重要）：
   - 自动判题：`CppFunctionExtractor` 按花括号配平提取类定义、判题模板 `{{STUDENT_CODE}}` 占位符替换、`[RESULT]`/`[SCORE]` 标记解析。
   - 题库 CDN 多镜像容错（GitHub raw + 两个 jsDelivr 镜像 + 8s 超时重试）。
   - 学习历史的字符级事件合并与双缓冲区刷盘策略。
   - 导出文件名中嵌入各题测试分数、仅导出「代码+音频+测试」三者齐备的题目。
5. **README 强调的依赖与实现的差异**：README 把「Monaco Editor」「MinGW-w64」作为并列技术栈，代码中 Monaco 通过 `@materia-ui/ngx-monaco-editor` 封装、MinGW 作为 `extraResources` 打包，二者均非 npm 常规依赖。
6. **Howler.js 的加载方式**：README 多次提到 Howler.js，但它是**运行时从 CDN 动态加载**（`cdnjs.cloudflare.com`），未列入 `package.json`，离线环境音频回放会失效。
7. **`chcp` 模块被绕过**：`basicUtil.ts` 里 `chcp` 以注释掉的 import 和临时替代实现存在（固定返回代码页 936），代码注释明示「临时替代实现，绕过 chcp 模块」。
8. **残留的模板痕迹**：项目源自 `angular-electron` 模板（README 已说明），代码中保留了模板的 `webFrame`/`remote` 导入、`main.ts` 中模板式注释，以及大量被注释掉的功能（文件菜单、项目管理、大纲按钮等）。

---

## 附：关键文件索引

| 关注点 | 文件 |
|--------|------|
| 主进程入口 / 窗口 | `main.ts` |
| IPC 类型定义（唯一来源） | `src/app/core/ipcTyping.d.ts` |
| 编译 / 运行 / 取消运行 | `src/background/handlers/build.ts` |
| GDB 调试 | `src/background/handlers/debug.ts` |
| DSALab 核心业务（题目/历史/判题/导出） | `src/background/handlers/dsalab.ts` |
| 配置 / MinGW 路径 | `src/background/handlers/store.ts`、`src/background/basicUtil.ts` |
| 编辑器封装 / 断点 / 历史采集 | `src/app/services/editor.service.ts` |
| 编译运行调度 | `src/app/services/build.service.ts` |
| 调试状态机 | `src/app/services/debug.service.ts` |
| 题目状态中枢 | `src/app/services/dsalab-problem.service.ts` |
| 历史记录（渲染侧） | `src/app/services/dsalab-history.service.ts` |
| 录音 + 题面渲染 | `src/app/main-view/sidebar/problem-description/problem-description.component.ts` |
| 导出交互 | `src/app/header/dsalab-control/dsalab-control.component.ts` |
| 音频播放器 | `src/app/shared/components/howler-audio-player/howler-audio-player.component.ts` |
| 题库数据 | `problem.json` |
| 打包配置 | `builder/*.json`、`main.webpack.js`、`angular.webpack.js` |
